export interface MenuCategory {
  id: string;
  name: string;
  description: string;
  image_url: string;
}

export interface BurgerCategory {
  id: string;
  name: string;
}
export interface Taste {
  id: string;
  name: string;
}
